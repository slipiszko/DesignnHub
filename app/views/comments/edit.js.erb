var comment = document.querySelector('#comment-<%= @comment.id %>');

comment.innerHTML = '<%= escape_javascript(render "edit_comment_form", locals: { design: @comment.design, comment: @comment }) %>'
