var selectedComment = document.querySelector('#comment-<%= @comment.id %>');

selectedComment.innerHTML = '<%= escape_javascript(render "edit_comment_form", locals: { design: @comment.design, comment: @comment }) %>'
