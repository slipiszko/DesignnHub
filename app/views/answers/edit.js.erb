var selectedAnswer = document.querySelector('#answer-<%= answer.id %>');

selectedAnswer.innerHTML = '<%= escape_javascript(render "edit_answer_form", locals: { question: @answer.question, answer: @answer }) %>'
