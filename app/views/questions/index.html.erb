<div class="d-flex">
  <%= render 'question_sidebar' %>
  <div>
    <div>
    <% @questions.each do |question| %>
      <%= render 'card_question', question: question %>
    <% end %>
    </div>
  </div>
</div>
